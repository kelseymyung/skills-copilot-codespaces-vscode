function skillsMember() {
  // 1. Get the member's skills
  const memberSkills = member.skills;

  // 2. Iterate over the member's skills
  memberSkills.forEach((skill) => {
    // 3. Display each skill
    console.log(skill);
  });
}